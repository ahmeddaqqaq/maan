"use client";

// This file is disabled due to missing StatisticsService
// All exports have been commented out to prevent build errors

// Placeholder to prevent import errors
export function useStatistics() {
  return { data: null, isLoading: false, error: null };
}

export function useProductionStatistics() {
  return { data: null, isLoading: false, error: null };
}

export function useFinancialStatistics() {
  return { data: null, isLoading: false, error: null };
}

export function useMonthlyStatistics() {
  return { data: null, isLoading: false, error: null };
}